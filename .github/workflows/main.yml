name: C/C++ CI

on: [push]

jobs:
  job_id:
     runs-on: ubuntu-latest
     name: foobar
     steps:
       - uses: actions/checkout@v1
       - name: configure
         env:
           autopull:  0
         run: |
           ls -la
           bin/fterm --no-term exec make -j2
           ls -la
           bin/fterm --no-term run --version
           
